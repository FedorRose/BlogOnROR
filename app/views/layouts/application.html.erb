<!DOCTYPE html>
<html>
  <head>
    <title>Blog</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%# <%= stylesheet_link_tag 'https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css' %> %>
    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>

    <div class="sidenav">
    <span class="magazine"><%= link_to "Magazine", home_path %></span>
      <ul class="menu">
          <% Cat.all.each do |cat| %>
        <li><a class="aa-sel"><%= link_to cat.title, cats_path(:cat_slug => cat.slug) %></a></li>
          <% end %>
        <li><br><br></li>
        <li>
          <% if user_signed_in? %>
            <span><%= current_user.email %></span>
            <%= link_to 'Logout', destroy_user_session_path, :method => :delete %>
          <% else %>
            <%= link_to 'Login', new_user_session_path %> or <%= link_to 'Signin', new_user_registration_path  %>
          <% end %>
          <br><br>
         </li>
         <li><% if user_signed_in? %>
           <%= link_to 'Add Page', add_path %>
         <% end %></li>
      </ul>
    </div>


        <%= yield %>

  </body>
</html>
